name: Scheduled Yacht Scan

on:
  schedule:
    # Every 6 hours: 00:00, 06:00, 12:00, 18:00 UTC
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: # Also allow manual trigger

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger scan on Vercel
        run: |
          echo "Running scheduled yacht scan..."
          echo "Note: Actual page scanning requires the browser extension or an external scraping service."
          echo "This workflow can be configured to call external scraping APIs in the future."
          
      # Future: Add Puppeteer/Playwright scanning here
      # - name: Run Playwright scanner
      #   uses: microsoft/playwright-github-action@v1
      #   with:
      #     script: scripts/scan.js
      #   env:
      #     API_URL: ${{ secrets.API_URL }}
      #     CRON_SECRET: ${{ secrets.CRON_SECRET }}